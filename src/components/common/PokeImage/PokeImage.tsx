import { FC, useEffect } from 'react';

interface PokeImageProps {
  url: string;
}

const PokeImage: FC<PokeImageProps> = ({ url }) => {
  useEffect(() => {}, []);
  return <></>;
};

export default PokeImage;
